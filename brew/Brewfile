# Residual Brewfile: only the things mise's built-in Homebrew support (bootstrap.packages)
# cannot do. Everything else moved to mise's [bootstrap.packages] / [tools] in
# mise/.config/mise/config.toml. Apply with: brew bundle --file=~/.dotfiles/brew/Brewfile
#
# Why each entry is here:
# - ollama: needs `brew services` (restart_service), which mise-brew does not implement.
# - parallel: needs `link: false` to avoid clobbering moreutils' parallel; mise-brew has
#   no link control.
# - The casks below are pkg-based / privileged / SDK installers that mise's narrow cask
#   support (app-bundle/binary/simple-pkg only) cannot install. gcloud-cli in particular
#   is referenced by its Caskroom path in zsh/.config/zsh/paths.zsh.

# Create, run, and share large language models (LLMs)
brew "ollama", restart_service: :changed
# Shell command parallelization utility
brew "parallel", link: false

# Set of tools to manage resources and applications hosted on Google Cloud
cask "gcloud-cli"
# Plugin for AWS CLI to start and end sessions that connect to managed instances
cask "session-manager-plugin"
# Network protocol analyzer
cask "wireshark-app"
# SQL powered operating system instrumentation and analytics
cask "osquery"
# Semantic code analysis engine
cask "codeql"
# Column-oriented database management system
cask "clickhouse"
# Monitor filesystem activity
cask "filemonitor"
# Monitor process activity
cask "processmonitor"
