# cm configuration. See https://github.com/chancez/cm/blob/main/docs/config.md

detach_key = "ctrl-\\"

scrollback_lines = 10000

# log_level = "debug"

[persist]
# Let session content survive a reboot. Off by default.
enabled = true
sessions = ["*"]
on_restore = "shell"
safe_commands = ["nvim", "less", "htop"]
